# TODO

- [x] Update `index.html`:
  - [x] Replace social media `<img>` links with inline SVG icons wrapped in `<a>` elements.
  - [x] Fix hamburger markup to avoid duplicate toggling and ensure `.menu-links` closing works reliably.
- [x] Update `style.css` for new social icon styling.
- [ ] Verify behavior:
  - [ ] Mobile hamburger open/close + clicking a menu link closes.
  - [ ] Social icons render (no broken images) and open correct URLs.
  - [ ] Experience bullet icons and About icons size/shape look correct.



